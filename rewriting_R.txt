var 
    type
    default
    options: null if no options
    required: bool
    restrictions
    info

no default value means its requried

formatting: 
    @param variable_name A(n) [type] ... Default Value is \code{variable_name = [" "]}. Options include: logical, just, default.units

Run devtools::document() in console to convert .R to .Rd

Rishabh: Genes, GenomeLabel, HiCs, Ideogram, Manhattan, Signal, MultiSignal, Raster, Rect, Segments
Abiye: Pairs, PairsArches, Ranges, Circle, Legend, Polygon, Text

If a param has a default value at the top of the file, include in rest of the file. If it does not, do not include it it.